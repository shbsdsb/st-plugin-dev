{
  "name": "client-find",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "前端插件清单生产端:扫描 st.client 生成 CLIENT_BOOT,统一 serve /plugins/*,provide clientBoot service",
  "main": "lib/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "cordis": "file:../../agent_plugin_feat/vendor/cordis"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.3",
    "vitest": "^4.1.11"
  },
  "st": {
    "bundle": { "patch": "./cordis.patch.yml" }
  }
}
