{
  "name": "chat-plugin",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "前后端一体插件:中央插槽气泡对话页,active 表单完整 prompt + history/input 动态注入,整轮回滚落库 sqlite(cordis v4)",
  "main": "lib/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json && node ../../agent_plugin_feat/bootstrap/src/cli/index.ts web-build --cwd .",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "cordis": "file:../../agent_plugin_feat/vendor/cordis"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.3",
    "vitest": "^4.1.11"
  },
  "st": {
    "bundle": { "patch": "./cordis.patch.yml" },
    "client": { "kind": "web", "path": "lib/index.cjs", "immed": true, "inject": ["ui-polish", "st-ui-slots", "ui-tool-plugin"] }
  }
}
